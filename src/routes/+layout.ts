/**
 * Витрина — статический сайт целиком.
 *
 * `prerender` собирает каждую страницу в готовый файл: GitHub Pages умеет
 * отдавать только файлы, и это ровно то, что нам нужно — открывается мгновенно
 * и не зависит ни от какого сервера.
 *
 * `ssr = false` для страниц с шейдером и формой: они всё равно оживают только в
 * браузере, а разметку без них выдаёт prerender.
 */
export const prerender = true;
export const trailingSlash = 'always';
